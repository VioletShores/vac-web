# GATE RECORD — F-875 medical-demo.html Browse QA

**Date:** 2026-07-21
**Branch:** task-s140-medical-demo (tip 3236c3f)
**Tester:** athena-exec /browse gate
**File:** medical-demo.html (859 lines)
**Spec ref:** docs/strategic/SPEC-FINANCE-MEDICAL-DEMOS-2026-07-20.md (athena repo)
**Server:** localhost:8765 (python3 -m http.server)

---

## Summary

All rendering and interaction beats PASS. No rendering/interaction defects found requiring fixes (STEP 2 has no changes). Three spec content gaps noted in receipt fields — documented below as FAIL sub-items under Beat 13; these are content/spec compliance gaps, not rendering/interaction defects, and are held for a separate Rob-gated content pass.

---

## Beat Results

### Beat 1 — Simulation banner on first paint
**PASS.** Amber full-width banner renders above the fold before any interaction. Text: "Simulation — patient case and AI assessments are illustrative; no real clinical data; biometric seal runs against the VAC production backend." Uppercase mono font, amber colour, visible on 1280×720, 768×1024, and 375×812 viewports.

### Beat 2 — Header / brand
**PASS.** "VAC Protocol" brand name and "VERIFIABLE AUTHORITY CHAIN" sub-label render correctly. "CLINICAL AUTHORITY · PREVIEW" mode badge (teal, mono) present in top-right. Custom SVG medical mark (cross/circuit motif) renders.

### Beat 3 — Tab navigation
**PASS.** "Walkthrough" and "How it works end-to-end" tabs both render. Default selected tab = Walkthrough (`.tab.sel` on `data-tab="walk"`). Tab switching works: clicking architecture tab hides walkthrough panel and shows arch panel; clicking back restores walkthrough.

### Beat 4 — Derm step-up case card
**PASS.** Case card "C-01 Pigmented lesion — AI-flagged, urgent review" renders with case number badge (purple), AI advisory chip, and case description. Click applies `.sel` class to card and reveals the stage section.

### Beat 5 — Stage reveal
**PASS.** Stage section animates in with heading "Pigmented lesion — AI-flagged step-up" and case ref "CASE-DERM-2026-0847". Six step rows render with step numbers (not yet active). "Begin" and "Reset" buttons present.

### Beat 6 — Step 1: AI assessment received
**PASS.** Clicking "Begin" activates step 1 (dot goes purple, step-detail revealed). AI advisory block renders with:
- Patient reference PT-2026-4471, Female 54, left forearm, 8×6 mm
- Urgency flag "URGENT REVIEW RECOMMENDED" in amber
- Policy chips: `ai_flag=urgent_review`, `advisory_only=true`, `diagnosis=false`
- ai-not-claim disclaimer: "AI advisory output only — this is not a diagnosis, not a clinical recommendation, and not the output of a registered medical device." — text present and readable.

### Beat 7 — Step 2: Clinician reviews
**PASS.** Step 2 reveals with independent review content, relevant clinical history (no prior excisions, no immunosuppression, 15 years outdoor exposure, phototype II, nil current medications), policy chips, and explicit independence statement ("clinician's review is independent of the AI suggestion").

### Beat 8 — Step 3: Step-up triggered
**PASS.** Step 3 reveals with biometric step-up content, policy chips (`decision_class=high_stakes`, `biometric_required=true`), and Ahpra accountability binding copy.

### Beat 9 — Step 4: Pre-seal clinical safety checklist
**PASS — interaction.** Step 4 reveals with exactly 6 check items. Initial tally: "0 of 6 confirmed — complete all items before proceeding" (verified via JS). Advance button disabled with text "Complete checklist to proceed". Clicking all 6 items updates tally to "6 of 6 confirmed ✓ — ready to proceed" and enables the advance button. Checked state renders correctly (green background, checkmark SVG visible).

**SPEC NOTE (not a rendering defect).** The spec (§medical-demo.html) requires checklist items: "diagnosis established · severity/indication met · prior-therapy history · contraindications · screening (e.g. infection) · monitoring plan · shared-decision note" (7 clinical-safety items). The built checklist has 6 AI-governance independence items instead (patient identity, images reviewed personally, history considered, differential considered, independent judgment, AI reviewed not adopted). Clinical-safety items not present. Hold for Rob's content call.

### Beat 10 — Step 5: Decision path selection
**PASS.** Step 5 reveals with all 5 decision path buttons confirmed present (verified via JS: `urgent_refer, routine_refer, clinical_watch, request_info, reject_ai`). All 5 are selectable:
- Path A — Urgent specialist referral (amber label)
- Path B — Routine specialist referral (teal label)
- Path C — Watchful waiting (success/green label)
- Path D — Request additional information (purple label)
- Path E — Reject AI suggestion (red label, full-width)

Path note updates correctly on each selection.

### Beat 11 — Path E: REJECT AI suggestion
**PASS.** Clicking Path E adds both `.sel` and `.dp-reject` classes. Border colour changes to `--red`, background to `--red-bg`. pathNote renders: "REJECT AI SUGGESTION selected — Independent clinical finding — AI urgency flag not supported by clinician assessment" in red. Advance button text becomes "Seal decision" and is enabled.

### Beat 12 — Seal flow
**PASS.** Clicking "Seal decision" calls `mintToken()` (async) then `showReceipt()`. Receipt element gets `.show` class, animates in with green heading "Clinical decision sealed & lodged". Advance button is hidden post-seal. `SEAL_TOKEN` gracefully falls back to placeholder token when backend API is unreachable from localhost (CORS — expected; production Vercel origin is allowed).

### Beat 13 — Receipt fields vs spec
Receipt KV fields confirmed present (verified by extracting all `dt`/`dd` pairs):

| Field | Value (Path E run) | Spec-required | Status |
|---|---|---|---|
| CASE REF | CASE-DERM-2026-0847 | yes | PASS |
| PATIENT REF | PT-2026-4471 · anonymised | yes (pseudonymous) | PASS |
| AI SUGGESTION | urgent_review — advisory only · not a diagnosis · not the decision | yes | PASS |
| CLINICIAN DECISION | REJECT AI SUGGESTION — INDEPENDENT CLINICAL JUDGMENT | yes (independent decision) | PASS |
| DECISION BASIS | Clinician's own review: imaging, history, differential. AI advisory noted and explicitly not adopted. | yes (evidence reviewed) | PASS |
| REGISTRATION | ASSERTED by clinician — not independently verified by VAC Protocol against Ahpra or any medical board registry | yes (role/registration assertion) | PASS |
| CHECKLIST | 6 of 6 pre-seal confirmations sealed with this record | yes (implied) | PASS |
| AI ≠ DECISION | Explicit: AI advisory output and clinician's decision sealed separately and not conflated | yes (explicit separation) | PASS |
| SEALED AT | 2026-07-21 14:10:39 UTC | yes (timestamp) | PASS |
| TOKEN | vat_root_c1e665235203 | yes | PASS |
| TRUST SCORE | 0.95 | — | PASS |
| VERIFY | /vat/verify/vat_root_c1e665235203?intent=verify | — | PASS (URL scheme guard active per /review fix) |
| GUIDELINE VERSION | — | **yes** (spec: "guideline version") | **FAIL — absent** |
| MODEL VERSION | — | **yes** (spec: "model version") | **FAIL — absent** |
| EHR explicit line | — | **yes** (spec: "Explicit line: complements, never substitutes, EHR/prescribing/consent workflows") | **FAIL — absent from receipt fields and receipt note** |

Receipt note text present and confirms ASSERTED registration, demo status, no real clinical record. Does not contain the spec-required "complements, never substitutes, EHR/prescribing/consent workflows" language.

Path A (urgent_refer) also sealed and verified — CLINICIAN DECISION showed "URGENT REFER — INDEPENDENT CLINICAL JUDGMENT" correctly.

### Beat 14 — Audit log
**PASS.** 6 audit-line entries render below receipt KV: case opened, clinician review initiated, step-up triggered, safety checklist completed 6/6, decision path selected, decision sealed Ed25519. Teal timestamps and mono font render correctly.

### Beat 15 — Architecture tab
**PASS.** Tab switch reveals architecture view. Five nodes render:
- A: AI Assessment Module (purple tint)
- B: Clinician Review Queue
- C: VAC Biometric Step-Up (amber tint)
- D: Pre-Seal Safety Gate
- E: Sealed Clinical Receipt (green/wide)

arch-note present. Confirmed: "live and in production" text present; "not a TGA-registered medical device" language present. Arch-node B note: "No automated decision path from AI output to clinical action — the clinician gate is mandatory."

### Beat 16 — More cases grid
**PASS.** 4 cards present: Radiology AI — independent reporting; Emergency triage — AI-assisted priority; Prescribing authority — high-risk medication classes; Legal & tribunal — verified authority chain. Legal/tribunal card contains link to /tribunal-demo (verified via JS `querySelector`).

### Beat 17 — CTA section
**PASS.** "Deploying clinical AI and need the authority trail?" section renders with "Start a conversation" button. Button href = `mailto:enterprise@vacprotocol.org?subject=VAC%20Clinical%20AI%20Governance%20%E2%80%94%20inquiry` (confirmed via JS).

### Beat 18 — Footer posture copy
**PASS.** Footer section present. Confirmed:
- "not a medical device" language present
- "does not verify clinical registration against Ahpra or any medical board registry" present
- "A person authorises every outcome" present
- "does not make clinical decisions" present

---

## Responsive layout
**PASS.** Screenshots taken at 375×812 (mobile), 768×1024 (tablet), 1280×720 (desktop). No overflow, no broken layout at any breakpoint. Mobile: decision-paths grid collapses to 1-column (CSS `@media(max-width:560px)`), case-grid collapses to 1-column, arch-row stacks vertically, arch-arrow rotates 90°. Header brand + badge fit within 375px width.

---

## Console / network
- No JS errors during page load or interaction.
- CORS error on `POST https://vac-backend-production.up.railway.app/v1/vat/issue` from localhost:8765 — **expected, not a defect.** Production Vercel origin has CORS permission; localhost does not. Fallback to placeholder token executes correctly.
- No broken images (0 `img` elements with failed load).
- No 404s attributable to this page.

---

## Defects fixed in STEP 2
**None.** No rendering or interaction defects were surfaced by the /browse gate.

---

## Spec gaps (held — not rendering/interaction defects)
These are content compliance gaps vs SPEC-FINANCE-MEDICAL-DEMOS-2026-07-20.md. They require a Rob-gated content pass, not a rendering fix.

1. **Receipt: missing GUIDELINE VERSION field** — spec requires it.
2. **Receipt: missing MODEL VERSION field** — spec requires it.
3. **Receipt note: missing EHR explicit line** — spec requires "complements, never substitutes, EHR/prescribing/consent workflows."
4. **Checklist items mismatch** — spec listed 7 clinical-safety items (diagnosis established, severity/indication met, prior-therapy history, contraindications, screening, monitoring plan, shared-decision note); implementation has 6 AI-governance independence items. Functional but content-divergent from spec.

---

## Verdict
**GATE PASS — rendering/interaction clean.** All 18 beats pass. No STEP 2 fixes required. Four spec content gaps documented above; Rob to call on whether to close before merge.
