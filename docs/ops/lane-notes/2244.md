# Task 2244 — grant page S179: return-nav + load-ux

**Branch:** `task-grant-page-return-and-load-ux-s179` (pushed 2026-08-31)

**Status:** COMPLETE — two fixes implemented, 51/51 tests pass, /review CLEAN, branch pushed. PR creation blocked: `gh auth login` unavailable in this environment; open https://github.com/VioletShores/vac-web/compare/task-grant-page-return-and-load-ux-s179 to create the PR manually.

**Fix 1 (return-nav, auth.html ~L1138):** Success CTA now recognises `?return=/grant` — adds `/grant(\.html)?` to the return-nav regex, sets `_hash=''` (no `#matters` appended), label `Return to grant →`. Tribunal-demo and financial-demo unchanged.

**Fix 2 (load-ux, grant.html):** `#stage` div carries a static `Checking session…` placeholder before JS runs, eliminating blank flash. `renderGate` note updated from stale "button at the bottom goes elsewhere" to "tap Return to grant → on the success screen".

**Tests:** 4 new source-level assertions (tests/grant-page.test.js:522–569). Commit: `8cfd5a1`.
