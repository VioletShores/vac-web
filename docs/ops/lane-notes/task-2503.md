# Lane note — task-2503 / PKT-S156 Matteo one-pager, Lane B prime (L-2679)

## Premise check (before building anything)

- `content/blocks/` — confirmed all 11 files present: vision, wedge, icp,
  architecture-l1, architecture-l2, privacy-ladder, gtm, papers, portfolio,
  sdk-story, INDEX. Not touched — used as source only.
- `content/blocks/INDEX.md` — confirmed it references `p/matteo-v2-*` as the
  one-pager v2 target.
- Live page `p/matteo-9be78161d6a7.html` — confirmed pre-build state was v1:
  `grep -oic accountability` → 0 hits.
- `content/blocks/portfolio.md` — confirmed canonical count is "1,068 claims
  across 17 filings"; no stale 1,028/16 anywhere in the block.

One thing the premise didn't mention and I want on record: a second file,
`p/matteo-v2-b8d2f4c1e39a.html`, already existed on disk (added same commit
that built the block library, well before this task). It was a working
assembly of the blocks — accountability-led, status badges, privacy ladder —
but it was never linked from anywhere in the repo (`grep -rn` for both
filenames across `*.html`/`*.md`/`*.json` returned nothing) and the link sent
to Matteo is the original `matteo-9be78161d6a7` slug, not that one. Since the
task's own preference is in-place rebuild specifically "so the existing sent
link stays valid," I treated that orphan file as a superseded draft rather
than the deliverable.

## What I assembled

Rebuilt `p/matteo-9be78161d6a7.html` **in place** (chose in-place over
create-v2-and-redirect — see below). Composed from `content/blocks/`:

- **vision** + **wedge** → new lead section "The accountability problem",
  h1 changed to "Accountability for AI agents — built the way we instrument
  a plant" (was "The trust layer for AI agents..."). EU AI Act retrofit
  (Article 50, 2 Aug 2026) called out as the wedge, ahead of the product
  walkthrough.
- **icp** → new "Who this is for first" section: legal/tribunal, water/
  critical infrastructure, defence-adjacent; one-irreversible-action-class
  entry framing.
- **architecture-l1** → the five capability cards now carry BUILT·LIVE /
  BUILT·MERGING status badges (previously unlabeled) and an added
  "Assurance proportional to the action" card that was folded into the
  SignalRank card before.
- **privacy-ladder** → new "On data privacy" section (Layer 1/2/3 + the
  SOC 2 / GDPR Art. 25 "planned Q4 2026, not claimed today" honesty line) —
  this section didn't exist in v1 at all; it answers Matteo's "how do you
  deal with data?" question from the original conversation.
- **gtm** → funding-stack paragraph added to "Where this is going" (Smart&Start
  Italia, Eurostars/Innosuisse with the Swiss carve-out via IDSIA, EIC
  Accelerator 2027).
- **papers** → IETF links switched from the self-hosted `/papers/*.txt`
  mirrors to the canonical `datatracker.ietf.org` URLs per the papers block's
  link policy.
- **portfolio** → claims-register link text now reads "1,068 claims across
  17 filings, receipt-verified" (was "1,068 patent claims, receipt-verified");
  market-moment paragraph likewise updated to "...patent estate across 17
  filings...".

Untouched from v1: the personal framing ("Why an instrumentation & control
engineer built this", Zero Trust section, "See the engines live" demo links,
Sicily close) — those aren't block-sourced content and didn't need rebuilding.

Deleted `p/matteo-v2-b8d2f4c1e39a.html` (the orphan draft described above) so
there isn't a second, diverging "true" version of this page sitting live at
a guessable URL after this change. Nothing referenced it.

## Rendered patent number

`p/matteo-9be78161d6a7.html` now shows **1,068 claims across 17 filings** in
two places (claims-register link, market-moment paragraph) — matches
`content/blocks/portfolio.md` exactly. No stale 1,028/16 anywhere on the page.

## In-place vs. v2+redirect — choice made

**In-place rebuild**, per the task's stated preference. Reason: the only
inbound link that matters here is the one already sent to Matteo
(`p/matteo-9be78161d6a7.html`); there's no nav entry or footer link pointing
at this page from the rest of the site (confirmed via repo-wide grep), so
there's no redirect-fanout risk to worry about, and rebuilding in place means
the sent link never needs to change. No reason found to prefer the
create-v2-and-redirect path.

## Other fixes

- `scoring.html`: checked the "certified" wording the task called out (VAC-level
  claim → should read "independently tested/validated", iBeta/NIST is a
  third-party **component** test). This was **already fixed** on `main` before
  this task started — line 146 reads "independently validated — iBeta Level 1
  PAD (third-party tested)..."; the only remaining use of "certif-" on the page
  is "VAC's own 30107 certification planned for Q4 2026," which is a specific,
  named, future component certification, not a present VAC-level claim. No
  change made; confirmed correct as-is.

## UI hard rules

- `.page-shell` background `#0A0F1A` (dark) — present.
- Anchors `#C9A227` (gold), not blue — present, unchanged from v1.
- Custom SVG icons only, no emoji/font-icons — page uses no icon imagery at
  all (text tags/status badges only), so nothing to swap; scanned for emoji
  range codepoints, none found.
- `<meta name="theme-color" content="#0A0F1A">` + Google Fonts `<link>` —
  both present.

## Language posture

- "in production" language used ("The system is in production — live backend,
  hundreds of sealed verifications..."); no "in market" claim anywhere on the
  page.
- Swiss carve-out present (IDSIA as the Swiss research anchor, Innosuisse
  route named separately from Eurostars).
- No competitor disparagement beyond the un-named "incumbent answer is
  screenshots and log-scraping" framing, which is verbatim from the wedge
  block (not written new for this page) — didn't touch that language per
  DO-NOT-REBUILD-BLOCKS instruction.
- SDK not mentioned on this page at all — nothing to check.

## Verification done

- Dead-link check at depth 2 (internal `vacprotocol.org`/relative links only,
  via a local script following `href`s from the rebuilt page and one hop
  further): **0 dead links**. The two IETF datatracker links are external and
  weren't checked over the network — used per the papers block's canonical-link
  policy as-is.
- HTML structure: balanced `<div>` open/close (19/19), Python `html.parser`
  walk reports no tag mismatches, no unclosed tags.
- `/code-review` (self-review): ran, no findings — this is a static content
  diff with no logic paths; checked HTML validity, CLAUDE.md vercel.json rule
  (doesn't apply — no new filename added, existing slug reused), and
  duplicate/orphan file cleanup.
- `/browse` visual QA: **not run** — no browser/screenshot tool is available
  in this execution sandbox (checked via ToolSearch, no Playwright/Puppeteer-
  equivalent found; no `/browse` skill in this session's available-skills
  list despite the general instructions mentioning one). Did confirm the file
  serves correctly (`python3 -m http.server`, HTTP 200, `page-shell` class
  present in the served bytes) as a functional smoke test, but that is not a
  substitute for an actual rendered visual pass. Flagging this explicitly per
  "if you can't test the UI, say so explicitly rather than claiming success"
  — **requesting a human or a session with browser tooling run `/browse`
  against this page before/after merge.**

## PR

Opened against `main` from `task-2503-matteo-v2-assemble`. No auto-merge (no
`ATHENA_PERMIT_JSON` present in this session's environment — verified it's
empty before doing anything, so this lane has no authority to merge and
didn't attempt to). Requested codex review + human review in the PR body,
and flagged the missing `/browse` pass above.
