# GREETING CANNOT-HEAR — PINPOINT DIAGNOSIS (S161, chat-Claude, for lane task-ceremony-greeting-hear)
**Rob live datum 13 Aug ~12:30-13:10 UTC:** mic meter FULLY GREEN while greeting refused ("can't hear"). Regression window: post-f2d05ac P0 merge. Worked ~2 weeks ago.
**Root cause (vac-reauth-ceremony.js ~4855-4885):** two captures disagree. Analyser (meter) hears Rob; SpeechRecognition content gate opens its OWN capture and hears NOTHING on Rob's device (contention — already documented in-code from Rob's t740 log). With `_sessionGateAvail && !_vadStarved`, the no-match escape requires `PHRASE_VOICED_TICKS_NEEDED * 2`; a normal greeting supplies ~1x → timeout one condition short. The t740 patch relaxed to 1.0x ONLY when `_vadStarved` — Rob's analyser is healthy, so multiplier stays 2.
**Fix shape (preserves the P0 security):** track whether the content gate has produced ANY transcript events (interim included). If ZERO transcripts while `_phraseVoicedTicks >= NEEDED*1.0` (+ modulation), the recognizer is DEAD (not mismatching) → set gate-dead, escape at 1.0x IMMEDIATELY (same as _vadStarved path), server verdict remains content authority on the recording (third-party audio still fails server-side match — security unchanged). Keep 2x ONLY when transcripts exist but mismatch (true wrong-content case). Emit vacDebug `phrase_gate_dead_escape` with tick counts.
**Fixture:** Rob-session replay: analyser voiced ticks ≥ needed, zero SR transcripts → greeting passes ≤1s after utterance end; wrong-content transcripts present → still strict.
